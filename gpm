#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/externals/gpm/bin/gpm" "$@"
  ret=$?
else 
  node  "$basedir/externals/gpm/bin/gpm" "$@"
  ret=$?
fi
exit $ret
